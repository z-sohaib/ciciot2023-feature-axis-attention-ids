# CICIoT2023 Data

Place CICIoT2023 CSV files here:

```text
data/raw/
```

The project accepts either:

- a single CSV file;
- multiple CSV files inside `data/raw/`;
- a nested extracted folder such as `data/raw/CSV_MERGED/*.csv`.

Do not commit raw dataset files to version control.
